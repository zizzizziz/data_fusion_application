<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ldn.cs.fusion.dao.ConveyDao">

    <!-- 新增ConveyInofs报表数据 -->
    <insert id="addConveyInfos" parameterType="ldn.cs.fusion.pojo.convey.Convey">
        INSERT INTO tbl_convey_info(corporation, type, goods, quantity, inventory, mileage, cost, eventTime, updateTime)
        VALUES
        <foreach collection="conveys" item="convey" separator=",">
            (#{convey.corporation}, #{convey.type}, #{convey.goods}, #{convey.quantity}, #{convey.inventory}, #{convey.mileage}, #{convey.cost}, #{convey.eventTime}, #{convey.updateTime})
        </foreach>
    </insert>

    <!-- 查询ConveyInfos报表 -->
    <select id="getConveyInfos" resultType="ldn.cs.fusion.pojo.convey.Convey">
        select * from tbl_convey_info
        <where>
            1=1
            <if test="statement != null and statement != ''">
                <if test="types == 1">
                    AND corporation = #{statement}
                </if>
                <if test="types == 2">
                    AND updateTime = #{statement}
                </if>
            </if>
        </where>
        order by updateTime DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 查询ConveyInfos报表Total -->
    <select id="getTotalConvey" resultType="int">
        select count(1) from tbl_convey_info
        <where>
            1=1
            <if test="statement != null and statement != ''">
                <if test="types == 1">
                    AND corporation = #{statement}
                </if>
                <if test="types == 2">
                    AND updateTime = #{statement}
                </if>
            </if>
        </where>
    </select>

    <!-- 新增Traffic数据 -->
    <insert id="addTrafficInfos" parameterType="ldn.cs.fusion.pojo.convey.Traffic">
        INSERT INTO tbl_convey_traffic_info(corporation, type, mileage, cost, eventTime, updateTime)
        VALUES
        <foreach collection="traffics" item="traffic" separator=",">
            (#{traffic.corporation}, #{traffic.type}, #{traffic.mileage}, #{traffic.cost}, #{traffic.eventTime}, #{traffic.updateTime})
        </foreach>
    </insert>

    <!-- 新增Inventory数据 -->
    <insert id="addInventoryInfos" parameterType="ldn.cs.fusion.pojo.convey.Inventory">
        INSERT INTO tbl_convey_inventory_info(corporation, goods, quantity, inventory, eventTime, updateTime)
        VALUES
        <foreach collection="inventories" item="inventory" separator=",">
            (#{inventory.corporation}, #{inventory.goods}, #{inventory.quantity}, #{inventory.inventory}, #{inventory.eventTime}, #{inventory.updateTime})
        </foreach>
    </insert>


    <!-- 查询Traffic数据 -->
    <select id="getTrafficInfos" resultType="ldn.cs.fusion.pojo.convey.Traffic">
        select * from tbl_convey_traffic_info
        <where>
            <if test="granularity != null and granularity != ''">
                <choose>
                    <when test="granularity == 1">
                        YEAR(FROM_UNIXTIME(eventTime / 1000)) = #{time}
                    </when>
                    <when test="granularity == 2">
                        QUARTER(FROM_UNIXTIME(eventTime / 1000)) = #{time}
                    </when>
                    <when test="granularity == 3">
                        MONTH(FROM_UNIXTIME(eventTime / 1000)) = #{time}
                    </when>
                </choose>
            </if>
        </where>
    </select>

    <!-- 查询Inventory数据 -->
    <select id="getInventoryInfos" resultType="ldn.cs.fusion.pojo.convey.Inventory">
        select * from tbl_convey_inventory_info
        <where>
            <if test="granularity != null and granularity != ''">
                <choose>
                    <when test="granularity == 1">
                        YEAR(FROM_UNIXTIME(eventTime / 1000)) = #{time}
                    </when>
                    <when test="granularity == 2">
                        QUARTER(FROM_UNIXTIME(eventTime / 1000)) = #{time}
                    </when>
                    <when test="granularity == 3">
                        MONTH(FROM_UNIXTIME(eventTime / 1000)) = #{time}
                    </when>
                </choose>
            </if>
        </where>
    </select>
</mapper>